// src/components/TopBar.jsx

import React from 'react';

const TopBar = React.createClass({

  render() {
    return (<div id="app-topbar">TopBar</div>);
  }

});

TopBar.propTypes = {};

export default TopBar;
